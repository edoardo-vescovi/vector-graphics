#include <stdlib.h>
#include <stdio.h>
#include	<gl\gl.h>										// Header File For The OpenGL32 Library
#include	<gl\glu.h>										// Header File For The GLu32 Library
#include <windows.h>
#include "GLtiming.hpp"
#include "GLbmp.hpp"
#include "console.h"

int main()
{
   
 /*   Timer timer;
    timer.enable();

    char str[20];
    int i;
    for(i=0;i<50;i++) {
      timer.grabStart();

      timer.getTimeStrHour(str);
      printf(str);
      printf("\n");
      timer.wasteUntil(0.1);
      timer.updateElapsed();
    }      
*/

  system("PAUSE");

}
